Creating folder using command promt-->mkdir foldername or git init nameoffolder
Creating folder using GitHub bash-->touch foldername

pwd ----> it is used to check  i am in which folder

1) creating the files in local repository(from pc) and uploading files into remote repository

open the GiHub bash in folder, which folder files you want to upload by right click on mouse
git init  --->initiaziting the Git in this folder means creating working repository
git add . --->adding the files in the folder into staged area for tracking
git status ---> checking the status
git commit -m "commit name" --->adding the files into remote repository

git log
       commit da3aa87eafd9e4f93979de1d3285bd3aaaaa85d8 (HEAD -> master)
       Author: Raghu M N <nraghu@zeomega.com> 
       Date:   Tue Nov 23 11:02:26 2021 +0530
git remote add origin url of your remote repository
git remote -v   ----->checking remote origin add or not
git push origin master
like this -->https://github.com/Raghumnstudent/Raju.git

2)Downloading  the file from remote repository into local repository (to your pc)

open the GiHub bash in folder, in which folder, the files are getting downloaded by right click on mouse
git init  --->initiaziting the Git in this folder means creating working repository
git add . --->adding the files in the folder into staged area for tracking
git status ---> checking the status
git commit -m "commit name" --->downloading  the files into local repository(

git log
       commit da3aa87eafd9e4f93979de1d3285bd3aaaaa85d8 (HEAD -> master)
       Author: Raghu M N <nraghu@zeomega.com> 
       Date:   Tue Nov 23 11:02:26 2021 +0530

git pull url of your remote repository (with url type .git at the end of url)
like this -->https://github.com/Raghumnstudent/Raju.git

3)Modifiying the already exsiting files, then uploading the files from local repository (pc) into remote repository

open the GiHub bash in folder, in which folder, the files are uploading by right click on mouse
git init
git add .
git status -->this shows the files which are modified
git commit -m "uploading the modified files"
git push url of your repo (with url type .git at the end of url)
like this -->https://github.com/Raghumnstudent/Raju.git

go to your repository --.click on branch-->click on new pull request-->click on create pull request
-->click on merge full request-->click on confirm merge

go to your repository


4)Deleting the files in local repository and restoring again into same folder

open the GiHub bash in folder, in which folder, the files are deleting and restoring by right click on mouse
git init  --->initiaziting the Git in this folder means creating working repository
git add . --->adding the files in the folder into staged area for tracking
git status ---> checking the status
git commit -m "commit name"

git log
       commit da3aa87eafd9e4f93979de1d3285bd3aaaaa85d8 (HEAD -> master)
       Author: Raghu M N <nraghu@zeomega.com> 
       Date:   Tue Nov 23 11:02:26 2021 +0530

git revert da3aa87eafd9e4f93979de1d3285bd3aaaaa85d8

esp :wq

git checkout HEAD~1 filename or (git restore --staged filename and then git restore filename)


5) creating a repository in GitHub using HTTP (this method is easy)

create repository manually in github
open the GitHub bash in the folder, which folder files you want to upload
In below, there is command like this ---> git remote add origin https://
copy that
git remote -v
git push url of ypur repository (with url type .git at the end of url)
like this -->https://github.com/Raghumnstudent/Raju.git


6) creating a repository in GitHub using SSH
git status
ls -lrt
ssh-keygen
after above command press three times Enter
copy the path it is in above like c://
cat copied path  --->this gives key link
copy that public key link
login into GitHub-->go to setting-->select SSH and GPG keys
click new SSh key --->give title then paste copied public key
click new repository -->give name
click on SSh protocal
copy the link looks like git remote add origin
execute that
git push origin master
yes for authentication for first time
go and refresh the page
cat index.html
git log --oneline
 



7)ignoring some files and uploading remaining files

open GitHub bash in the folder, which folder files you want to upload
type--> touch .gitignore
From above command .gitignore file is created, open that and type file name you want to ignore with extention like raghu.css
git init
git add .
git commit -m "uploading"
git push url of your repository (with url type .git at the end of url)
like this -->https://github.com/Raghumnstudent/Raju.git


